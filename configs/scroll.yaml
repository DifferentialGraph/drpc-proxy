archive:
  - id: ${ID}
    chain: scroll
    labels:
      provider: ${PROVIDER}
      archive: true
    connection:
      generic:
        rpc:
          url: ${RPC}
        ws:
          frameSize: 20Mb
          msgSize: 50Mb
          url: ${WS}
